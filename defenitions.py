import os

ROOT_DIR = os.path.dirname(os.path.abspath(__file__))  # This this is the project root dir.
TIME_SERIES_ARRAY_SIZE = 100  # This is the array size of the number of weight plots used for clustering
METHOD = 'single'  # hierarchical cluster distance method used to determine how to cluster the nodes.
